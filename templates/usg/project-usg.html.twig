{% extends 'usg/default-usg.html.twig' %}
{% block content %}
<div id="list">
    <ul>
        {% for item in pages.find('/usg19/project').collection %}
        <li><span class="project-list">
            <a href="{{ item.url }}" class="item">
                {% set img = item.media[item.header.picked_image] %}
                <img class="tooltip-img" src="{{ img.resize(img.height >= img.width ? 350 : 500, auto).url }}"/>
                <span class="tooltip-text">{{ item.title }}{% if item.header.subtitle is not empty %}, {{ item.header.subtitle }}{% endif %}.</span>
            </a>
        </span></li>
        {% endfor %}
    </ul>
</div>

<div id="project-container">
    <a href="#" id="cross">✕</a>
    <header class="menu-bar">
        <nav>
            <ul>
                <li><a href="#">< Projet précédent</a></li>
                <li><a href="#">Projet suivant ></a></li>
            </ul>
        </nav>
    </header>
    <article class="project green">

    </article>
    <aside class="menu-bar right">
    </aside>
    <footer class="menu-bar">
        <p></p>
    </footer>
</div>
{% endblock %}

{% block javascripts %}
{% do assets.addJs('theme://js/script-usg19.js') %}
{% endblock %}
